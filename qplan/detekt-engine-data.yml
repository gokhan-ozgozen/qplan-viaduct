# Config for the type-resolution detekt tasks wired in model/build.gradle.kts. Only the custom
# rules below are active here.
engine-data-rules:
  EngineOutputDataOverride:
    active: true
  EngineUnionMembership:
    active: true
    reportUnknown: false
